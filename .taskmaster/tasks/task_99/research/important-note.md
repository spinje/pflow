we should not just make claude code node use the pflow nodes as tools but also expose pflow workflows in the same way.

this is another input to the node with another mcp server being added if more than one workflow is added.