# 09b-cache.below-min-observed

Trace-backed runtime warning replay. The trace records provider telemetry with
0 cache creation/read tokens plus a catalog-backed runtime warning; the
baseline locks that `analyze-cache --from-trace` rehydrates
`cache.below-min-observed`.
