# 20-llm.thinking-temperature-mismatch

LLM node sets `thinking_effort` but `temperature` is non-default (provider requires temperature=1 with extended thinking). Triggers `llm.thinking-temperature-mismatch` (warning).
