# 09c-cache.below-min-rendered

Trace-backed runtime warning replay for pre-send marker stripping. The trace
contains `cache_skipped_reason="below_min"` and a catalog-backed warning; the
baseline locks `cache.below-min-rendered` in `analyze-cache --from-trace`.
