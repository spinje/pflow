# 09-cache.below-min-predicted

Tiny input-backed chunk + sonnet (1024 min). The command passes `article=hi`
so the warning fires from real parameters-tier cacheable-token evidence, not
from the deleted declared-cache heuristic.
