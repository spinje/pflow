# Cache Analysis: workflow.pflow.md
  File: .taskmaster/tasks/task_159/baseline/04-warning-catalog/05b-cache.sub-workflow-cache-undeclared-subpath/workflow.pflow.md

  Workflow: 2 LLM nodes using anthropic/claude-sonnet-4-5
  (0 in workflow.pflow.md, 2 in 1 sub-workflow)

## Summary

  Cost per run:                unavailable

  1 recommended action

  Cost data unavailable: run the workflow once for cost figures.
  Suggested:  pflow run .taskmaster/tasks/task_159/baseline/04-warning-catalog/05b-cache.sub-workflow-cache-undeclared-subpath/workflow.pflow.md article=<value>

## Recommended actions (1, ordered by impact)

  Each item below is a cache-optimization opportunity for this workflow.
  Declared values are sent once and reused at 0.1× input cost.

  For each "Sub-workflow cache undeclared" finding, apply ALL THREE edits in the listed child workflow. Doing only some leaves the cache disabled:
    (1) Remove the `${var}` references from each affected node's prompt. Leaving them re-sends the content uncached.
    (2) Add the input as a named entry under the child workflow's ## Cache section.
    (3) Reference that named entry in `prompt_cache:` on each consumer node.

  1. Sub-workflow cache undeclared in child.pflow.md — declare 1 input   saves ~$0.02/run
     child.pflow.md
     1 value flows in from parent workflow.pflow.md, used by 2 consumer nodes.
     Per-consumer cache prefix (what the provider sees per call):
       Node `summarize` (~3,207 tokens per call — above 1,024-token minimum):
         • `article` ~3,207 tokens — uses `${article.title}`
       Node `translate` (~3,207 tokens per call — above 1,024-token minimum):
         • `article` ~3,207 tokens — uses `${article.body}`
     → Edit: sub/child.pflow.md

## Per-child analyze-cache commands

  Sub-workflow opportunities don't surface here — run analyze-cache per child:
    cd .taskmaster/tasks/task_159/baseline/04-warning-catalog/05b-cache.sub-workflow-cache-undeclared-subpath
    pflow analyze-cache sub/child.pflow.md

## Notes

  · Per-call cache report hidden — workflow has no run data yet. Run once, then re-run analyze-cache for real per-node token estimates and cacheable projections.
  · Cross-workflow projections require trace evidence to populate per-call rows. Recommended actions still surface; run with `--from-trace <path>` for per-call attribution.
