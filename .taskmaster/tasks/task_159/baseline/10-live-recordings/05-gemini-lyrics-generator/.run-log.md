# Recording log — 2026-05-08

**Recorder**: claude-opus-4-7 (audit task), under Andreas's direction.

**Setup**:
- Saved original `default_model: anthropic/claude-haiku-4-5`
- Set `default_model: gemini/gemini-2.5-flash` via `pflow settings llm
  set-default`
- Source: first 3000 chars of `_shared/long-stable-text.txt`
- Output: `/tmp/pflow-lyrics-<timestamp>` (output files not committed)

**Command** (executed live, NOT via run-case.sh):
```bash
HOME="$REAL_HOME" GEMINI_API_KEY=... \
  uv run pflow run \
  _shared/workflows/lyrics-generator/lyrics-generator.pflow.md \
  sources='["<3000 chars from long-stable-text.txt>"]' \
  output_base=/tmp/pflow-lyrics-<ts>
```

**Run summary**:
- Wall clock: 580.5s (9:40)
- Cost: $2.3113
- LLM calls: 253 (per `llm_summary.total_calls`)
- Total tokens: 2,006,570 (input 1,189,076 + output 817,494)
- Models used: gemini/gemini-2.5-flash, gemini/gemini-2.5-flash-lite,
  gemini/gemini-3-flash-preview
- All 4 songs completed successfully; all 60 output files saved
- Zero retries, zero rate-limit errors

**Trace fixture preparation**:
- Raw trace: 53MB (excessive duplication of prompt text across
  4 fields per event)
- Trimmed: 12MB. Trim strategy:
  - Replaced `llm_prompt`, `llm_response`, `llm_system` with markers
  - Stripped `template_resolutions` (debug-only, very large)
  - Replaced strings >1000 chars in `node_output` and `node_params`
    with `<trimmed N chars>` markers
- Trimmed trace produces same analyze-cache output (verified via
  run-case.sh --diff after re-capture)

**Cleanup**:
- Restored `default_model: anthropic/claude-haiku-4-5`
- Saved 4 generated songs at `/tmp/pflow-lyrics-<ts>/` (left for
  Andreas to inspect or delete)

**Re-recording**:
- See `README.md` for the full recording flow.
- Trim script: see `BASELINE-AUDIT.md` section L-8 commit history.
- Re-record only when LiteLLM cache-translation behavior changes,
  trace format bumps, or workflow itself changes.

**Findings surfaced by this recording**: 9 audit findings (L-1
through L-9 in `BASELINE-AUDIT.md`), including 3 critical code
bugs that no static analysis case could have found:
- L-1: trace mode discards observed_models when default_model unset
- L-2: per-call rows show IR-declared model not trace's observed
- L-3: first-run delta calculation disconnected from actual savings
