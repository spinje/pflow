# Cache Analysis: workflow.pflow.md
  File: .taskmaster/tasks/task_159/baseline/10-live-recordings/03-gemini-translation/workflow.pflow.md

  Workflow: 2 LLM nodes using gemini/gemini-2.5-flash
  Evidence: complete trace (2 LLM nodes executed)

## Summary

  Actually paid:               ~$0.0013 (from trace)
  Cost without caching:        ~$0.0046
  Cost on rerun (within TTL):  ~$0.0013
  Actual savings (this run):    saves ~$0.0033/run vs no-cache, 71% of no-cache cost
  Rerun delta (projected):      saves ~$0.0033/run on rerun, 71% of no-cache cost

## Per-call cache report
  How to read each row:
    · cached_now: tokens served from cache during this run. `—` if no trace was recorded.
    · could_cache: extra cacheable tokens if you declared/extended `prompt_cache:`. `?` if the analyzer couldn't project; `—` if cached_now already has the measured number.
  Showing 0 of 2 LLM nodes; low-signal rows hidden (--all-rows shows everything).

  Hidden: 2 low-signal nodes (no warnings or actionable cache projection; rerun with --all-rows).

## Notes

  · Cache fidelity check skipped: this is a first run (no prior runs to compare cached vs uncached against). On later runs, specific cache misfires (TTL expired, chunks skipped) will appear here.
  · Gemini caching: Gemini reports cache reads via 'cache_read_input_tokens' in trace events. Cache writes don't show in telemetry, so verify caching is working by checking that follow-up calls show cache reads. Explicit ## Cache declarations are required for Gemini.
