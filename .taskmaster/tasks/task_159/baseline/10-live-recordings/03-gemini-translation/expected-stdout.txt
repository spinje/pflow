# Cache Analysis: <BASELINE_CASE_DIR>/workflow.pflow.md

  Workflow: 2 LLM nodes using gemini/gemini-2.5-flash
  Evidence: complete trace (2 LLM nodes executed)
  Confidence: high_from_trace (2 of 2 nodes)

## Summary

  Actually paid:               ~$0.0013 (trace)
  Cost without caching:        ~$0.0046
  Cost on rerun (within TTL):  ~$0.0013
  Actual savings (this run):    saves ~$0.0033/run vs no-cache, 71% of no-cache cost
  Rerun delta (projected):      saves ~$0.0033/run on rerun, 71% of no-cache cost

## Per-call cache report
  Actual cache ratios from declared `prompt_cache:` subsets. cached_now: tokens that went through cache this run; could_cache: tokens that could be cached if you declare/extend prompt_cache:. — means the column does not apply to this row's tier. Numbers aggregate across all calls when calls > 1 (divide by calls for per-call values).
  Showing 0 of 2 LLM nodes; all-clean rows hidden (--all-rows shows everything).

  Hidden: 2 nodes at ≥80% projected cache ratio with no warnings (rerun with --all-rows).

## Notes

  · Discrepancy detection: predicted-key matching unavailable (memo cache empty — predicted-key matching needs prior memo cache entries to compare against). Observable-field attributions (TTL expiry, chunk skipped) still apply.
  · Gemini telemetry note: LiteLLM's Vertex/Gemini translation surfaces explicit-cache reads via 'cache_read_input_tokens' (or 'prompt_tokens_details.cached_tokens'); 'cache_creation_input_tokens' is 0/absent even when caching is working. Verification path is reads on subsequent calls — explicit cache markers are required for caching to fire on Gemini.
