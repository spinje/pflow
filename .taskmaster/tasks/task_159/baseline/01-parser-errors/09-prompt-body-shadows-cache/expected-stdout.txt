# Cache Analysis: workflow.pflow.md
  File: .taskmaster/tasks/task_159/baseline/01-parser-errors/09-prompt-body-shadows-cache/workflow.pflow.md

  Workflow: 1 LLM node using anthropic/claude-haiku-4-5

## Summary

  Cost per run:                unavailable

  1 error blocking
  1 recommended action

  Cost data unavailable: run the workflow once for cost figures.
  Suggested:  pflow run .taskmaster/tasks/task_159/baseline/01-parser-errors/09-prompt-body-shadows-cache/workflow.pflow.md article=<value>

## Cache blocking errors (must fix before save and run)

  1. Prompt body duplicates cached chunks on summarize (`cache.prompt-body-duplicates-cache`)
     summarize
     Node 'summarize' duplicates cached chunks in the prompt body — the cache stores these values at 0.1× rate but the body sends them inline at 1.0× every call:
       - cached `${article}` AND inline `${article}`
     → Remove the listed `${...}` references from the prompt body — the cached chunks already supply these values via the system prompt.

## Recommended actions (1)

  Each item below is a cache-optimization opportunity for this workflow.
  Declared values are sent once and reused at 0.1× input cost.

  1. Declared cache content below provider minimum on summarize          savings unavailable
     summarize
     summarize: declared cache content is ~1 tokens, below anthropic/claude-haiku-4-5's minimum of 4096; cache_control markers will silently no-op at the provider
     → Increase cache content above 4096 tokens by adding more chunks to ## Cache, OR remove `prompt_cache:` from summarize since the cache won't fire as declared.

## Per-call cache report
  How to read each row:
    · could_cache: extra tokens that would be cached if you declared/extended `prompt_cache:`. `?` if not projectable statically. Numbers below your model's provider minimum won't cache — see notes column.

  node       model                       input  could_cache  ratio  notes
  ------------------------------------------------------------------

  summarize  anthropic/claude-haiku-4-5     12            1     8%  below-min-predicted; prompt-body-duplicates-cache

  Token estimate confidence:
    · Projected input tokens for: summarize.
