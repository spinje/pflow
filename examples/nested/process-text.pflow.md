# Process Text

A simple sub-workflow that normalizes text based on the given mode.
Used as a nested workflow by other examples.

## Steps

### normalize

Normalize the input text according to the specified mode.

- type: test
- operation: ${mode}
