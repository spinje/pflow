# No File References (Regression Test)

A workflow with no file references — should work identically to before.

## Steps

### greet

Say hello.

- type: shell

```shell command
echo "Hello world, no file references here"
```
