# Package marker: gives _generate.py a distinct pytest module name
# (react_flow_contracts._generate) — --doctest-modules otherwise collides it
# with cache_analysis/_generate.py (same basename, both non-package dirs).
