# Cache Analysis: /Users/andfal/projects/music-generation/workflows/lyrics-generator/song-creator/chorus-chooser/chorus-chooser.pflow.md

  3 LLM nodes using anthropic/claude-sonnet-4-5 + generate-chorus-options (model varies per batch item)

## Summary

  Current cost per run:        unavailable
  Optimized cost per run:      unavailable
  Cost on rerun (within 1h):   unavailable

  2 opportunities (0 warnings, 2 info)

  Absolute cost figures need a prior run. Run the workflow once, then re-run analyze-cache for real cost figures and cacheable projections.

## Recommended actions (ordered by impact)

  Each item below is one edit that unlocks LLM-provider caching.
  Declared values are sent once and reused at 0.1× input cost.

  1. Shared context undeclared — declare `concept.core_idea` in ## Cache  savings unavailable
     chorus-chooser.pflow.md
     Used by 2 LLM nodes. Chunks: concept.core_idea.

  2. Prompt opaque to static analysis on generate-chorus-options — refactor inline for cache detection  savings unavailable
     generate-chorus-options
     generate-chorus-options: prompt is `${item.prompt}`, assembled by `build-grouped-items` (type: code). Static analysis cannot inspect the assembled prompt's structure, so cache opportunities (shared prefixes, prewarm) are not detected. If the assembled prompt has a stable prefix and per-call dynamic content, refactoring to an inline declarative prompt unlocks detection.

## Suggested ## Cache block — /Users/andfal/projects/music-generation/workflows/lyrics-generator/song-creator/chorus-chooser/chorus-chooser.pflow.md

  Paste between ## Inputs and ## Steps. The labels above each `${var}` are
  auto-generated starters — replace each with a 1-2 sentence description
  of what that value is in your workflow's domain. The LLM reads this
  prose right before the value, so a clearer label helps the LLM
  understand what it's looking at.

  ## Cache

  - ttl: 5m

  ```cache
  The core idea from concept:

  ${concept.core_idea}

  ```

  Per-node `prompt_cache:` declarations

  Add to each node's params. Order MUST match the ## Cache block above —
  pflow rejects mismatched orders as `cache.order-mismatch` ERROR.

  ### score-choruses
  - prompt_cache: [concept.core_idea]

  ### select-chorus
  - prompt_cache: [concept.core_idea]

## Per-call cache report
  Projected cache ratios from prior run data.
  Showing 1 of 3 LLM nodes; all-clean rows hidden (--all-rows shows everything).

  generate-chorus-options               model=<varies>                            tokens=    3  cacheable=    0  ratio=  0%  src=low  opaque-prompt
