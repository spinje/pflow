# Cache Analysis: /private/tmp/brownfield-test.pflow.md

  1 LLM node using anthropic/claude-sonnet-4-5

## Summary

  Current cost per run:        unavailable
  Optimized cost per run:      unavailable
  Cost on rerun (within 1h):   unavailable

  1 opportunity (1 warning, 0 info)

  Absolute cost figures need a prior run (output token counts come from memo cache or a 2.1.0 trace). Run the workflow once and re-run analyze-cache.

## Recommended actions (ordered by impact)

  1. `prompt_cache:` order mismatch on write-lyrics                      savings unavailable
     write-lyrics
     Node 'write-lyrics' prompt_cache order doesn't match ## Cache declaration
       expected:  [a, b]
       you wrote: [b, a]
       fix:       reorder the `prompt_cache:` field to match ## Cache declaration order

  2. Cache content below provider minimum on write-lyrics                savings unavailable
     write-lyrics
     write-lyrics: declared cache content is ~1 tokens, below anthropic/claude-sonnet-4-5's minimum of 1024; cache_control markers will silently no-op at the provider

## Per-call cache report
  Actual cache ratios from declared `prompt_cache:` subsets.

  write-lyrics                          model=anthropic/claude-sonnet-4-5         tokens=    6  cacheable=    1  ratio= 17%  src=medium  below-min-tokens, order-mismatch

## Notes

  · Suggested-blocks: workflow already declares ## Cache; steady-state (partial-block) suggestions deferred to v1.x.
