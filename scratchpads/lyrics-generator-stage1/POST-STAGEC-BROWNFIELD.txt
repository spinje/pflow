# Cache Analysis: /private/tmp/brownfield-stage-c.pflow.md

  2 LLM nodes using 2 models: anthropic/claude-sonnet-4-5, ollama/some-fictional-model

## Summary

  Current cost per run:        unavailable (ollama/some-fictional-model lacks pricing data)
  Optimized cost per run:      unavailable (ollama/some-fictional-model lacks pricing data)
  Cost on rerun (within 1h):   unavailable (ollama/some-fictional-model lacks pricing data)

  1 opportunity (1 warning, 0 info)

  Absolute cost figures need a prior run. Run the workflow once, then re-run analyze-cache for real cost figures and cacheable projections.

## Recommended actions (ordered by impact)

  Each item below is one edit that unlocks LLM-provider caching.
  Declared values are sent once and reused at 0.1× input cost.

  1. Cache content below provider minimum on llm1                        savings unavailable
     llm1
     llm1: declared cache content is ~1 tokens, below anthropic/claude-sonnet-4-5's minimum of 1024; cache_control markers will silently no-op at the provider

## Per-call cache report
  Actual cache ratios from declared `prompt_cache:` subsets.
  Showing 1 of 2 LLM nodes; all-clean rows hidden (--all-rows shows everything).

  llm1                                  model=anthropic/claude-sonnet-4-5         tokens=    4  cacheable=    1  ratio= 25%  src=medium  below-min-tokens

## Notes

  · Suggested-blocks: workflow already declares ## Cache; steady-state (partial-block) suggestions deferred to v1.x.
