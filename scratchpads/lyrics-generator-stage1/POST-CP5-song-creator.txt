# Cache Analysis: /Users/andfal/projects/music-generation/workflows/lyrics-generator/song-creator/song-creator.pflow.md

  7 LLM nodes using 2 models: anthropic/claude-sonnet-4-5, gemini/gemini-3.1-pro-preview

## Summary

  Current cost per run:        unavailable
  Optimized cost per run:      unavailable
  Cost on rerun (within 1h):   unavailable

  4 opportunities (0 warnings, 4 info)

  Absolute cost figures need a prior run (output token counts come from memo cache or a 2.1.0 trace). Run the workflow once and re-run analyze-cache.

## Recommended actions (ordered by impact)

  1. [cache.shared-context-undeclared]  savings unavailable
     Workflow: song-creator.pflow.md
     `concept.core_idea, concept.title, concept.angle, concept_brief, creative-direction.response, song-architecture.response, easter-eggs.response` is referenced by 7 LLM nodes in this workflow but no ## Cache block declares it. Each call sends those tokens fresh; declaring them caches the bytes once and reuses at 0.1× input cost.
  2. [cache.shared-context-undeclared]  savings unavailable
     Node: choose-chorus
     `concept` flows from this workflow into sub-workflow `chorus-chooser.pflow.md` where 6 LLM nodes use it; no ## Cache block declares it on either side. Add it to either workflow's ## Cache to share cached bytes across the boundary.
  3. [cache.shared-context-undeclared]  savings unavailable
     Node: emotional-reviews
     `concept_brief` flows from this workflow into sub-workflow `review-emotional-architecture.pflow.md` where 6 LLM nodes use it; no ## Cache block declares it on either side. Add it to either workflow's ## Cache to share cached bytes across the boundary.
  4. [cache.shared-context-undeclared]  savings unavailable
     Node: craft-reviews
     `concept_brief` flows from this workflow into sub-workflow `review-accuracy.pflow.md` where 6 LLM nodes use it; no ## Cache block declares it on either side. Add it to either workflow's ## Cache to share cached bytes across the boundary.

## Suggested ## Cache block — /Users/andfal/projects/music-generation/workflows/lyrics-generator/song-creator/song-creator.pflow.md

  Paste between ## Inputs and ## Steps:

  ## Cache

  - ttl: 5m

  ```cache
  <DESCRIBE concept.core_idea — appears verbatim in cached system prefix>

  ${concept.core_idea}

  <DESCRIBE concept.title — appears verbatim in cached system prefix>

  ${concept.title}

  <DESCRIBE concept.angle — appears verbatim in cached system prefix>

  ${concept.angle}

  <DESCRIBE concept_brief — appears verbatim in cached system prefix>

  ${concept_brief}

  <DESCRIBE creative-direction.response — appears verbatim in cached system prefix>

  ${creative-direction.response}

  <DESCRIBE song-architecture.response — appears verbatim in cached system prefix>

  ${song-architecture.response}

  <DESCRIBE easter-eggs.response — appears verbatim in cached system prefix>

  ${easter-eggs.response}

  ```

  Per-node `prompt_cache:` declarations

  Add to each node's params. Order MUST match the ## Cache block above —
  pflow rejects mismatched orders as `cache.order-mismatch` ERROR.

  ### creative-direction
  - prompt_cache: [concept.core_idea, concept.title, concept.angle, concept_brief]

  ### easter-eggs
  - prompt_cache: [concept.core_idea, concept.title, concept.angle, concept_brief, creative-direction.response, song-architecture.response]

  ### generate-suno-prompt
  - prompt_cache: [creative-direction.response]

  ### rewrite-craft
  - prompt_cache: [concept.core_idea, concept.title, concept_brief, creative-direction.response, song-architecture.response]

  ### rewrite-emotional
  - prompt_cache: [concept.core_idea, concept.title, concept.angle, concept_brief, creative-direction.response, song-architecture.response, easter-eggs.response]

  ### song-architecture
  - prompt_cache: [concept.core_idea, concept.title, concept.angle, concept_brief, creative-direction.response]

  ### write-lyrics
  - prompt_cache: [concept.core_idea, concept.title, concept.angle, concept_brief, creative-direction.response, song-architecture.response, easter-eggs.response]

## Sub-workflow boundaries

  When this workflow calls a sub-workflow, values that flow across can be
  cached on either side. Each finding below describes one missed opportunity.

  ▸ song-creator → chorus-chooser  (via choose-chorus)
    `concept` is used by 6 LLM nodes across the boundary; no ## Cache block on either side.
    → Add `concept` to either workflow's ## Cache block.
    [cache.shared-context-undeclared]

  ▸ song-creator → review-emotional-architecture  (via emotional-reviews)
    `concept_brief` is used by 6 LLM nodes across the boundary; no ## Cache block on either side.
    → Add `concept_brief` to either workflow's ## Cache block.
    [cache.shared-context-undeclared]

  ▸ song-creator → review-accuracy  (via craft-reviews)
    `concept_brief` is used by 6 LLM nodes across the boundary; no ## Cache block on either side.
    → Add `concept_brief` to either workflow's ## Cache block.
    [cache.shared-context-undeclared]

## Per-call cache report
  Current ratios (always 0% pre-cache); see Recommended actions for the recoverable opportunities.

  write-lyrics                          model=gemini/gemini-3.1-pro-preview       tokens= 3289  cacheable=    0  ratio=  0%  src=medium
  song-architecture                     model=anthropic/claude-sonnet-4-5         tokens= 2555  cacheable=    0  ratio=  0%  src=medium
  rewrite-emotional                     model=gemini/gemini-3.1-pro-preview       tokens= 2315  cacheable=    0  ratio=  0%  src=medium
  rewrite-craft                         model=gemini/gemini-3.1-pro-preview       tokens= 2180  cacheable=    0  ratio=  0%  src=medium
  generate-suno-prompt                  model=anthropic/claude-sonnet-4-5         tokens= 1147  cacheable=    0  ratio=  0%  src=medium
  creative-direction                    model=anthropic/claude-sonnet-4-5         tokens= 1137  cacheable=    0  ratio=  0%  src=medium
  easter-eggs                           model=anthropic/claude-sonnet-4-5         tokens=  927  cacheable=    0  ratio=  0%  src=medium
